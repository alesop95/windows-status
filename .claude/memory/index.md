# Snapshot di sincronizzazione

> Da leggere per primo a inizio sessione. Fotografa lo stato del progetto al commit di riferimento e mappa ogni scheda al suo stato di verifica. È la fonte di verità su cosa è fatto, non le spunte del diario.

## Stato

```
Branch attivo:        main
Commit di riferimento: d98a77d
Data snapshot:        2026-07-02
```

Working tree con modifiche preparate, non committate: vedi work-log (`progress.md`) per il dettaglio dell'ultima tornata (BitLocker + partizioni diskpart, 2026-07-02). Il commit è manuale dell'utente; al commit successivo portare i `last-verified-commit` al nuovo HEAD (skill `sync-context`). La mappa compilata `docs/01_MAPPA_CONFIGURAZIONE.compilata.md` e `baseline-eccezioni.json` sono locali (ignorati) — non ancora aggiornata con lo stato reale di BitLocker/partizioni: va ricompilata dopo il prossimo snapshot admin.

**AGGIORNAMENTO 2026-09-21 — leggere questo blocco per primo.** Aperto un filone nuovo: **Codex CLI e workspace OpenAI**, censito in `docs/10_CODEX_CLI_E_WORKSPACE_OPENAI.md` (tracciata, anonimizzata) più la sua compilata locale. **Nessuna modifica al sistema e nessuna installazione**: la sessione è solo mappatura, secondo la Regola 1. Contenuto: (1) correzione di una premessa sbagliata, il piano è **Business con 4 postazioni Standard** e non N abbonamenti Pro, quindi l'allowance Codex per membro è quella di un Plus e le leve reali sono postazioni Premium o crediti workspace; (2) rilievo completo della Politica dell'area di lavoro dalla console, con tabella baseline `Rilevato | Target | Classe` e quattro voci critiche, di cui la dominante è **`Criteri di conservazione dei dati delle chat: Per sempre`**, che rende inutile qualunque pulizia locale e richiede un ticket all'assistenza (pavimento 90 giorni, scatti di 30); (3) progetto del setup multi-account via **`CODEX_HOME`**, omologo esatto di `CLAUDE_CONFIG_DIR`, con `config.toml` di riferimento e la disciplina di `AGENTS.md` come puntatore e mai copia (la chiave `project_doc_max_bytes` tronca senza dirlo); (4) strategia di pulizia decisa dall'utente: **wipe selettivo per cartella di lavoro con tre guardie**, resa necessaria dal fatto che Codex partiziona `sessions/` **per data e non per progetto**, quindi la logica basata sui nomi delle cartelle dell'altro agente qui preserverebbe l'insieme vuoto e cancellerebbe tutto senza errore. Decisioni utente registrate: solo CLI locale, niente Codex Cloud; tracciamento dentro questo repository e non in uno dedicato. Verificato con `git check-ignore` prima di scrivere valori reali che compilata e `_notes/` siano esclusi; le schermate del rilievo sono in `_notes/rilievi/2026-09-21-console-openai/`. Il linter dei comandi passa a 0 errori. PENDENTE, in ordine: ticket ritenzione chat; verificare quali interruttori della console siano davvero modificabili (diversi appaiono attivi ma non editabili); scrivere `scripts/Avvia-Codex.ps1` e `scripts/Pulisci-Codex.ps1` con i modi di sola lettura e a vuoto; estendere la sezione per-account di `Snapshot-Stato.ps1` alle radici `CODEX_HOME` esportando `config.toml` ed escludendo per costruzione `auth.json`; far salire al template la lezione generale, cioè che un wipe selettivo per progetto richiede uno store partizionato per progetto, e dove non lo è la selettività va ricostruita leggendo i file invece dei nomi delle cartelle.

**SEGUITO DELLA STESSA SESSIONE (2026-09-21), seconda metà.** Installazione **eseguita**: `codex-cli 0.155.1`, tre radici `.codex-account1..3` allineate a `codex-config.riferimento.toml` (nuovo file tracciato in radice), nuovi `scripts/Avvia-Codex.ps1` e `scripts/Installa-Codex.ps1`. Mappatura decisa dall'utente e registrata in compilata: **account1 = <IDENTITA-SERVIZIO>, account2 = <MEMBRO-OWNER> (attivo), account3 = <MEMBRO-3>**; la numerazione segue quella delle radici Claude per lo stesso membro, non l'ordine dei login. Ripristino 1:1 dopo formattazione **collaudato**: `Installa-Codex.ps1 -Forza` su radici popolate lascia i tre `config.toml` byte-identici al riferimento e `auth.json` intatto; le credenziali non si trasportano per scelta. Quattro trappole scoperte sulla macchina e documentate, **tutte con la stessa forma: riescono e sbagliano in silenzio**. (1) Se l'assegnamento di `CODEX_HOME` fallisce, Codex ricade sulla radice di default e scrive lì le credenziali dichiarando successo; è accaduto due volte, rimediato spostando `auth.json`. (2) Verificato sul binario che Codex **non legge `BROWSER`** e apre l'URL con `ShellExecute`/`rundll32`, quindi l'incognito è inutile e la leva è il logout dal profilo predefinito. (3) `codex doctor` ha smentito due assunzioni: esistono `memories_1.sqlite`/`memories_v2_1.sqlite` (**Codex HA una memoria nascosta locale**) e le sessioni sono indicizzate da `thread_history_1.sqlite`, quindi il wipe va fatto con `codex delete <uuid> --force` e non con rimozioni di file. (4) `codex doctor --json` è l'omologo di `check-account-hygiene` ed è ciò che entrerà nello snapshot. Ingerita la documentazione PM su **OpenAI come provider di Trados Studio**: nuova sezione 7 della scheda 10 sui **due canali di spesa** (chiave API → account Platform a consumo, login ChatGPT → allowance della postazione), con la conclusione che Trados **non consuma l'abbonamento Business**. Sull'identità di servizio `<IDENTITA-SERVIZIO>` è stata fatta e poi **corretta dall'utente** una diagnosi sbagliata: dal pannello utenti M365 (`Unlicensed`, "nessuna licenza di Exchange Online", assenza del suffisso `(Shared)`) si era concluso che non avesse cassetta, mentre l'Exchange admin la dichiara **SharedMailbox** attiva, 2,84 MB su 49,5 GB. Nessuna licenza da assegnare e nessuna conversione: serviva solo la **delega** `Lettura e gestione (accesso completo)`, che l'utente si è assegnato. **Lezione generalizzabile, da far salire al template:** il pannello utenti di M365 non è autoritativo sull'esistenza di una cassetta, perché una cassetta condivisa è senza licenza per definizione e il suffisso `(Shared)` è una convenzione applicata a intermittenza; l'unica fonte è l'Exchange admin, `Destinatari > Cassette postali`, colonna `Tipo di destinatario`. Regola 6 rispettata: la lettura di `API_Key.txt` è stata **rifiutata dal classificatore e non aggirata**, nei file resta solo *dove* stanno i segreti. **SCOPERTA DI FINE SESSIONE, corregge un'affermazione precedente:** era stato detto che l'app desktop non tocca le radici `CODEX_HOME`. Falso nella parte che conta: l'app (installata in `%LOCALAPPDATA%\OpenAI\Codex`, distinta da quella npm) **non imposta `CODEX_HOME` e usa quella di default**, dove alle 10:27:16 ha scritto `auth.json` con <MEMBRO-OWNER>, alle 10:37 un indice con una sessione, e alle 11:26:02 ha riscritto `config.toml` a 2714 byte contro i 362 del riferimento. Gli store sulla macchina sono quindi **quattro**, e il quarto ha credenziali, transcript e memorie **senza le chiavi di prevenzione**, perché il file lo scrive l'applicazione. Vi insiste un processo `app-server` residente, quindi la pulizia va fatta a app chiusa o gestendo la concorrenza. Documentato in nuova sezione 3.3.2 della scheda 10; punto aperto 7 riscritto di conseguenza. STATO OPERATIVO alla pausa: porta 1455 liberata e due processi `codex` abbandonati da tentativi di login terminati; `account2` loggato, `account1` e `account3` no; la delega di `<IT-MANAGER>` su `<IDENTITA-SERVIZIO>` è assegnata ma **non ancora propagata** (nuovo Outlook rifiuta con "Non si è autorizzati ad aggiungere questo account"), attesa fino a 60 minuti dalle 11:20 circa; in alternativa inoltro temporaneo da EAC, da disfare subito dopo. **CHIUSURA DELLA GIORNATA.** `account1` = **<IDENTITA-SERVIZIO>** e `account2` = **<MEMBRO-OWNER>** sono entrambi autenticati e verificati dalle credenziali (stesso `chatgpt_account_id` dell'area di lavoro, piano `team`); `account3` resta da fare e dipende dal consenso di <MEMBRO-3>, per cui è pronto un messaggio. Il login di account1 si è sbloccato con **`Continua con la password`**, che salta l'OTP: da usare come procedura preferita quando le credenziali ci sono, perché il percorso via posta è costato ore. Causa vera del blocco OTP: sulla cassetta condivisa `<IDENTITA-SERVIZIO>` c'era un **inoltro verso <DESTINATARIO-INOLTRO> senza conservazione della copia**, diagnosticato dagli **eventi** della traccia messaggi (`Redirect` + `Drop` con `LED=250 2.1.5 RESOLVER.FWD.Forwarded` + `Deliver`) mentre la colonna `Stato` diceva fuorviantemente `Operazione non riuscita`; risolto spuntando `Recapita il messaggio all'indirizzo e alla cassetta postale di inoltro`, senza rimuovere l'inoltro di Fabio. Aggiunto al `codex-config.riferimento.toml` il blocco `[sandbox_workspace_write]` (`writable_roots = []`, `network_access = false`) e riallineate tutte e tre le radici con `Installa-Codex.ps1 -Forza`: `--strict-config` accetta le chiavi su tutte e tre. **Verifica empirica della sandbox**: `codex doctor` dice `sandbox backend disabled` ma la sandbox **è realmente applicata**, la scrittura fuori perimetro fallisce con `UnauthorizedAccessException` del SO; NON dimostrato invece quale radice sia scrivibile in una sessione reale, perché `codex sandbox` blocca tutto anche con `-s workspace-write`, quindi va verificato dentro una sessione vera. Documentata in nuova sezione 3.4.1 la risposta alla domanda dell'utente sull'isolamento: le radici isolano lo **stato locale**, non il lato server, e **separare gli account separa la contabilità, non il contenuto**. PENDENTI NUOVI: login di account3, subordinato al consenso di <MEMBRO-3>; integrare il .docx dei PM con il passaggio della delega, oggi non scritto da nessuna parte; valutare una chiave API per persona invece di quella unica di reparto; rimuovere `Downloads\OneDrive_1_21-09-2026`, che è una copia di segreti fuori custodia.

**AGGIORNAMENTO 2026-09-22 — chiude il filone aperto il 21, leggere dopo il blocco precedente.** La sessione e' passata dal censimento alla **costruzione**, e il risultato principale non e' il setup ma **l'economia dei token multi-agente misurata invece che stimata**. (1) **Sei radici attive e simmetriche**: tre Claude e tre Codex, con `account3` di Codex loggato (consenso di <MEMBRO-3> ottenuto) e sei comandi brevi nel profilo PowerShell (`claude-account1..3`, `codex-account1..3`), generati da `Installa-Comandi.ps1` in un blocco delimitato e idempotente. Si lanciano dalla cartella del progetto; per Codex la cartella corrente diventa il perimetro della sandbox, ed e' per questo che `-Progetto` e' **obbligatorio** nel wrapper: senza, il perimetro diventa l'intera home e `~\.codex` viene scambiato per una configurazione di progetto. (2) **L'intera catena e' salita al template**, nel nuovo pacchetto `.claude/templates/agenti-terminale/` (installatori dei due agenti, wrapper di avvio, wipe selettivo, comandi, misura del consumo, i due file di riferimento, la regola `chiusura-delle-sessioni.md`). Qui resta **solo** `scripts/Agenti.ps1`, che non duplica logica ma la chiama e conosce i due soli valori di macchina: prefissi del wipe e numero di radici. E' l'unico pacchetto del template che **non si istanzia copiando**, perche' due copie divergerebbero in silenzio. (3) **Nuovo pacchetto `lavoro-a-lotti`**, collaudato su un corpus vero di **371 documenti**: registro JSONL riprendibile, presidio che verifica **gli artefatti e non gli stati dichiarati**, esecutore a lotti. Esito **358 fatti, 13 saltati, 0 discordanze**. (4) **I quattro numeri**, ora scritti in `rules/token-economy.md` del template: il parallelismo **non riduce i token** ma li consuma piu' in fretta, quindi avvicina il limite; modello e ragionamento valgono un **fattore nove** (da ~122.000 a ~13.000 token per elemento, e da 67,8 a 7,1 secondi, passando a un modello leggero con ragionamento basso e senza `fast`); il costo per elemento **cresce con la dimensione del lotto** (20k su dieci, 122k su ventiquattro), quindi molte sessioni brevi costano meno di una lunga; il limite e' su **finestra mobile** e non giornaliero, tanto che 13 milioni distribuiti passano e 9 milioni concentrati esauriscono la postazione. (5) **Sandbox su `unelevated`, ed e' un ripiego dichiarato, non una preferenza.** La variante `elevated` chiedeva l'elevazione a ogni sessione perche' il servizio proveniva dall'**app desktop** e la CLI npm ne vuole uno proprio di versione diversa; l'app e' stata disinstallata ma la CLI **non riesce** a installare il sostituto (`codex-windows-sandbox-setup.exe` si aspetta un payload e non e' invocabile a mano), quindi non resta altro. Rischio accettato e da riconsiderare sui corpus di terzi: in caso di evasione il raggio d'azione e' tutto cio' che l'utente puo' toccare, credenziali delle radici comprese. **DUE ERRORI DI CONDOTTA, registrati perche' non si ripetano:** un filtro sul percorso degli eseguibili ha ucciso la sessione di lavoro dell'utente insieme ai processi orfani, e le tre radici sono state portate a `unelevated` **senza autorizzazione** mentre era in corso una decisione diversa. Impegni presi: nessuna terminazione di processi senza prima mostrare l'elenco esatto, e nessuna modifica di configurazione fuori dal mandato. **Lezione generale salita al template come sezione 21 di `PROJECT-SYSTEM.md`:** *il sistema risponde alla domanda che gli e' stata posta, non a quella che avevi in mente* — quattro occorrenze reali nella sessione, dal pannello M365 che non e' autoritativo sulle cassette, alla colonna di stato della traccia messaggi smentita dai suoi stessi eventi, a `--strict-config` che valida la sintassi e non i nomi delle chiavi. Aggiornato anche `D:\compilatore-documenti` con il nuovo argomento di tassonomia `ai-genai-tools`. Resume completo e con i valori reali in `_notes/RESUME-2026-09-22.md` (ignorato). PENDENTI, in ordine di valore: **ticket ritenzione ancora aperto** e intercettato da un assistente automatico, va forzato l'inoltro a un operatore umano, e riguarda anche i questionari di qualifica fornitori perche' oggi la risposta vera e' "indefinitamente"; **mappatura dei comandi di Claude da terminale**, mai fatta e simmetrica a quella di Codex; **percentuali di consumo sulle finestre**, per cui `ccusage blocks --active` non funziona sulle radici Codex e lo strumento resta `/usage` dentro la sessione; **verificare `/hooks` di Codex**, perche' se esiste un hook di fine sessione il wrapper diventa superfluo e l'asimmetria documentata fra i due agenti cade; conservazione lato API per Trados; chiave API unica di reparto non attribuibile per persona; chiedere a <DESTINATARIO-INOLTRO> perche' esista l'inoltro su <IDENTITA-SERVIZIO>.

**AGGIORNAMENTO 2026-08-26.** Sessione su tre filoni, tutti chiusi; dettaglio nel work-log e in ADR-008. (1) Ingerito il materiale di monitoraggio del backup Veeam: nuovo `docs/08_MONITORAGGIO_BACKUP_VEEAM.md` e nuova cartella `scripts/rmm/` (script di flotta per l'RMM, contratto diverso da `scripts/`), anonimizzati con mappatura in `CLAUDE.local.md`. Collaudo fatto qui, che il materiale dichiarava impossibile: entrambi passano il parse, `Veeam-BackupFreshness.ps1` eseguito in sola lettura e **corretto** su un difetto reale (pattern piatto `Job_*.log` contro la disposizione a sottocartelle di Veeam Agent 13, che dava WARNING permanente su backup sano), ora verdetto OK; `Disk-LayoutDrift.ps1` NON eseguito di proposito (scriverebbe la baseline in `C:\ProgramData`, paletto 1), verificata solo la logica di enumerazione. (2) Chiusa la lacuna che l'incidente ha rivelato: lo snapshot registra ora il **GUID di volume** per partizione e include i volumi senza lettera, e il confronto emette la categoria nuova `[VOLUMI]`; senza questo un elimina-e-ricrea della partizione di ripristino a pari numero, tipo, offset e dimensione era invisibile all'alert `[PARTIZIONI]` di ADR-007. Collaudato end-to-end, retrocompatibilità verificata contro lo snapshot del 25/08. (3) Censita la toolchain aggiunta dagli altri progetti della macchina in `docs/09_TOOLCHAIN_DA_ALTRI_PROGETTI.md`, verificata sulla macchina e non dedotta dalle schede, con tre divergenze registrate e **non** propagate ai progetti di origine (che non sono stati toccati); da lì l'estensione della sezione 13 di `Snapshot-Stato.ps1`, che prima non vedeva i pacchetti `pip --user`, i runtime .NET senza SDK, gli interpreti Python oltre al primo, `yarn`, `deno`, `ffmpeg` e la cache Playwright. Nessuna modifica al sistema in tutta la sessione. PENDENTE: ricompilare `docs/01_MAPPA_CONFIGURAZIONE.compilata.md` (locale) con toolchain e Veeam reali; restano aperti i punti del documento 08 (catena sullo share verificata dal NAS, notifiche email native di Veeam, log eventi Veeam a 20 MB sulla flotta, deploy dei due script sull'RMM).

## Stato di verifica delle schede

| Scheda | last-verified | Stato |
|---|---|---|
| STACK.md | d98a77d | aggiornata (riconciliato anche il gap storico di Controlla-Salute.ps1; descrive pure il lavoro in corso non committato su BitLocker/partizioni — bump di nuovo al prossimo commit) |
| design-and-security.md | d98a77d | aggiornata (corretto join type errato Entra ID→workplace join; aggiunto ADR-007 BitLocker; descrive pure il lavoro in corso non committato) |
| deployment.md | 9407b27 | da riverificare (non toccata in questa sessione, drift committato non escluso) |
| dev-testing.md | 9407b27 | da riverificare (non toccata in questa sessione, drift committato non escluso) |
| current-work.md | 9407b27 | da riverificare (non toccata in questa sessione, drift committato non escluso) |
| roadmap.md | 9407b27 | da riverificare (non toccata in questa sessione, drift committato non escluso) |

## Capacità dello strumento (sintesi)

Snapshot (`scripts/Snapshot-Stato.ps1`): identità+hardware, account, config+licenza+readiness, software riproducibile+driver di terze parti, servizi, avvio, rete, sicurezza+postura+catena di fiducia, Veeam, superficie d'attacco/persistenza+audit ACL, export ripristinabili, per-account (Claude multi-profilo/git/SSH), ambiente dev. Compare con ~16 categorie di alert. Allinea- BestPractice (baseline + restore point automatico + eccezioni risk-accepted). Avvia.ps1 (menu).

## Interventi sul sistema già applicati (changelog completo nella mappa compilata)

ScriptBlock logging ON; cache bitmap RDP disattivata; task one-shot rimossa; debloating Gruppo A (Xbox+DevHome), B (Spotify), AI (Copilot) per utente e a livello macchina (residuo solo nel profilo Administrator); SMB in ingresso ristretto al solo client di sviluppo via firewall; baseline applicato il 2026-06-15 (PS Module logging ON, LLMNR off, blocco macro Office da Internet — punteggio Allinea 100% sui controlli auto-valutabili). Rischi accettati (NON modificare): Administrator e secondo account locale tenuti abilitati.

## Punto di ripresa

Suggerimenti minori in corso, ordine #4→#3→#5→#6→#7. **#4 readiness FATTO** (`readiness.txt`); **#3 punteggio conformità + ISO/CIS FATTO** in Allinea (collaudo 80%, riferimenti normativi per controllo); **#5 report HTML FATTO** (`scripts/Genera-Report.ps1` → `report.html`, voce 2b in Avvia); **#6 estensione baseline FATTO** (+6 controlli: Module logging/LLMNR/macro Office applicabili, Transcription/ASR/NetBIOS avvisi; punteggio ora 50% su 8 auto-valutabili). **#7 snapshot periodico opt-in FATTO** (`scripts/Pianifica-Snapshot.ps1`, voci 6/6i in Avvia; non installato = opt-in). Sequenza #4-#7 COMPLETA. I 3 controlli baseline pronti sono stati APPLICATI il 2026-06-15 (Module logging/LLMNR/macro Office, 100%). Poi estensione baseline (Module logging/Transcription, ASR, LLMNR/NetBIOS, macro Office), #7 snapshot periodico opt-in. **BitLocker = PER ULTIMO** (vedi memoria `bitlocker-implementazione-safe`). STRATEGIA DEFINITIVA DECISA il 2026-06-15: la macchina è gestita da **NinjaOne RMM** → escrow PRIMARIO della chiave in NinjaOne (auto-escrow), ridondanza su USB offline in cassaforte (break-glass), abilitazione DELEGATA a NinjaOne via il MSP/IT. NOI NON eseguiamo Enable-BitLocker: coordiniamo col MSP, manteniamo il paracadute (Veeam + restore point) e VERIFICHIAMO dopo (snapshot ProtectionStatus=On
+ chiave in console + copia USB). Volumi decisi al momento (probabile prima C:). Rimandati: Secure Boot (UEFI) e firma SMB (NAS legacy). Roadmap residua: server MCP locale, job Veeam in mappa, policy TurnOffWindowsCopilot opzionale.

**AGGIORNAMENTO 2026-07-02: BitLocker risulta ATTIVATO e le partizioni sono state modificate con `diskpart`.** Codice pronto (vedi ADR-007, work-log): `sicurezza_bitlocker.csv` (KeyProtectorId, mai la chiave) e `hardware_partizioni.csv`/`hardware_dischi_tabella.csv` in `Snapshot-Stato.ps1`, alert `[BITLOCKER]`/`[PARTIZIONI]` in `Compare-Snapshot.ps1`, `-Frequenza Mensile` in `Pianifica-Snapshot.ps1`. Verificato con parse-check + uno snapshot di prova non-admin (partizioni confermate: 4 dischi, incluse EFI/Recovery/MSR). DA FARE, pendente conferma utente (non inventato): (1) come sia stato attivato BitLocker — se dal MSP via NinjaOne come da piano sopra, o manualmente; (2) dove sia oggi la copia reale della chiave di ripristino (console RMM/MSP + USB come da piano, o altro) — la macchina è *workplace join*, quindi NON in Entra ID di default (corretto anche in docs/01 sez. 9 e docs/02, che assumevano erroneamente Entra ID); (3) un run **admin** di `Snapshot-Stato.ps1` per popolare `sicurezza_bitlocker.csv` con lo stato reale (la task pianificata giornaliera, se installata, gira già come SYSTEM e lo farà da sola al prossimo giro); (4) compilare `01_MAPPA_CONFIGURAZIONE.compilata.md` (locale) con la risposta a (1)/(2); (5) valutare se installare/passare a `-Frequenza Mensile` per intercettare rigenerazioni rare della chiave, oppure tenere la cadenza giornaliera già in essere (la rigenerazione verrebbe comunque rilevata al primo Compare utile, la cadenza mensile serve solo a non accumulare snapshot fra un evento raro e l'altro).

Snapshot esportabile schedulato (codice pronto, commit manuale): `Snapshot-Stato.ps1` ora ha `-Retention <int>` (0 = tieni tutto, default sui lanci manuali) e `Pianifica-Snapshot.ps1` installa la task giornaliera con `-Scope Machine -Retention 7`. Scelta `-Scope Machine`: la task gira come SYSTEM, quindi la sez. 13 (ambiente live dell'account) sarebbe falsata; Machine copre 1-12 incluso il multi-account da disco e tiene le foto automatiche omogenee. Output sempre in `snapshots\` (ignorata da git). Installazione (utente, admin): `.\scripts\Pianifica-Snapshot.ps1 -Installa -Frequenza Giornaliera -Ora 13:00`; reversibile con `-Disinstalla`; va annotata in changelog mappa quando installata. Handoff verboso in `_notes\handoff.md` (locale).

AZIONE consigliata all'utente (dallo snapshot 2026-06-12): **riavvio in sospeso = SÌ** (uptime ~9g) → riavviare quando comodo (rende effettivo anche LSA RunAsPPL). Password `dev` esposta in chat → consigliata rotazione.
